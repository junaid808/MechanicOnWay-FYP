<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mechaniconway1">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MechanicOnWay1">
        <activity android:name=".EmailAdmin"></activity>
        <activity android:name=".CancelAppointment" />
        <activity android:name=".BookingDetails" />
        <activity android:name=".DieselForm" />
        <activity android:name=".PetrolForm" />
        <activity android:name=".BatteryForm" />
        <activity android:name=".RimForm" />
        <activity android:name=".TyreForm" />
        <activity android:name=".NewBrakeForm" />
        <activity android:name=".BrakeReapirForm" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Call" />
        <activity android:name=".DieselRates" />
        <activity android:name=".PetrolRates" />
        <activity android:name=".Rims" />
        <activity android:name=".TyreWheelService" />
        <activity android:name=".EngineOilForm" />
        <activity
            android:name=".BrakeRates"
            android:label="@string/title_activity_brake_rates"
            android:theme="@style/Theme.MechanicOnWay1.NoActionBar" />
        <activity android:name=".Battery" />
        <activity android:name=".EngineOil" />
        <activity android:name=".AboutUs" />
        <activity android:name=".SignUpMechanic" />
        <activity android:name=".SettingActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MechanicMapsActivity"
            android:label="@string/title_activity_mechanic_maps" />
        <activity android:name=".MechanicLogIn" />
        <activity android:name=".ValidationScreen" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".CustomerMapsActivity"
            android:label="@string/title_activity_customer_maps" />
        <activity android:name=".Petrolium" />
        <activity android:name=".Brakes" />
        <activity android:name=".Tyre" />
        <activity android:name=".Parts" />
        <activity android:name=".BookAppointment" />
        <activity android:name=".EngineServices" />
        <activity android:name=".ActivityOne" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>